@model IEnumerable<PublicTransportNavigator.Models.BusSeat>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Bus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SeatType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CoordX)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CoordY)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastModified)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Bus.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SeatType.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CoordX)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CoordY)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastModified)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
